# Architecture

TelecomNE Grid Asset Links is organized as a two-service MVP: a Next.js frontend and a FastAPI backend. The backend uses SQLModel and can run against PostgreSQL/PostGIS in Docker or SQLite for local fallback.
